module.exports = ({ config }) => ({
  ...config,
  module: {
    ...config.module,
    rules: [
      {
        test: /\.css$/,
        use: [
          {
            loader: "style-loader"
          },
          {
            loader: "css-loader"
          },
        ]
      },
      {
        test: /\.(ttf|woff|woff2|eot|svg|png|jpg|gif)$/,
        loader: 'url-loader',
        options: {
          limit: 4096,
          name: './fonts/[name].[ext]?[hash]',
        },
      },
      {
        test: /\.md$/,
        use: [{
            loader: 'file-loader',
            options: {
                name: '[name].[ext]',
                outputPath: 'docs/'
            }
        }]
      }
    ],
  },
});